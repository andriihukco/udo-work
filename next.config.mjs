/** @type {import('next').NextConfig} */
const nextConfig = {
  // Serverless-friendly configuration for Vercel deployment
  experimental: {
    // Enable server actions if needed in future
  },
};

export default nextConfig;
